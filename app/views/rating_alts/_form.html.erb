<%= form_for(@rating_alt) do |f| %>
  <% if @rating_alt.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rating_alt.errors.count, "error") %> prohibited this rating_alt from being saved:</h2>

      <ul>
      <% @rating_alt.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :facility_id %> <br />
    <%= f.select :facility_id, Facility.all.collect { |x| [x.name, x.id] } %>
  </div>

  <div class="field">
    <%= f.label :rate %><br />
    <%= f.number_field :rate %>
  </div>
  <div class="field">
    <%= f.label :comment %><br />
    <%= f.text_field :comment %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
